# FormSnap V1 连续实施记录

开始日期：2026-09-09。起点 `17db808`，分支 main。既有 Phase 0 / Phase 1 不重写。
用户当前指令授权按阶段独立提交并连续完成 V1，覆盖此前“完成 Phase 1 后停止”的边界。

## 起始审计

- 已有 Task / Source Repository、Room v2、URI 持久授权、来源失效保留与 47 项测试。
- TableSchema / Field / Row / Cell 仅领域模型；尚无候选入库、质量检查、复核、识别、导出生产路径。
- 旧 Cell 默认 NEEDS_REVIEW 不能表达尚未检查，本次演进为 UNVALIDATED，不把候选自动判为需复核或已确认。
- 未跟踪的 `gradle/gradle-daemon-jvm.properties` 为用户已有内容（Java 25）；保留且不加入阶段提交。
- 当前没有 ADB 连接设备。保留历史安装阻塞；当前授权允许设备可用后尝试安装验收。
- 三份外部产品文档只读；正式产品文档的修改仍由主对话负责。

## 依赖与验收顺序

1. **动态结构与候选存储**：Room v3，Schema / Field / Row / Cell 稳定关联，真实来源归属，候选未检查状态，页级事务替换和保留数据的迁移。
2. **质量检查与复核事务**：纯领域 Validation、规则配置、多 Issue 聚合、决策持久化、重复提示、任务与导出门禁。先证明状态和数据一致性，再接入识别结果。
3. **真实识别与批量执行**：有明确适用范围的本机离线表格读取；保留区域与可靠性证据；处理失败必须可见。长任务使用 AndroidX 后台调度并支持重新执行。
4. **完整用户闭环**：规则配置、复核队列、原图局部和整页、搜索/编辑、记录排除、来源重处理、XLSX 保存及首页摘要。
5. **成品验证**：两种不同 Schema、800 Cell 标注 fixture、自动质量报告、迁移链、恢复与 UI 流程测试、最终 build/lint、文档差异报告和真实设备验收。

各段均执行相关测试、Debug 构建和必要 Lint，检查差异后独立 commit。
测试中的合成候选不计为真实识别准确率或真实手写验收。

## 阶段 2 设计：动态结构与候选存储

新增四张表。每个任务一个动态 Schema；字段按 ID 关联，显示顺序独立于原始列序。
复合外键防止 Row / Cell / Field / SourceDocument 串任务或串 Schema。来源删除级联删除对应行和 Cell，不影响原图。
一页候选在一个事务内替换；保留同来源同原行列的稳定 ID。新的 raw 是这次识别的原始候选，normalized / confirmed 均为空。
重复执行不追加重复行；如果已有人工确认、无法确认或排除记录，必须显式允许丢弃人工工作，不能静默覆盖。
页面表头与来源列身份不一致时拒绝入库，后续处理层会将此结果持久化为可处理的页面问题。
候选完成仅保持 PROCESSING，必须经过后续质量检查才可能进入可导出状态。
本段只新增真实可测试的数据能力，用户界面随后接入，不能视为 V1 已完成。

### 阶段 2 验证结果

2026-09-09：`testDebugUnitTest assembleDebug lintDebug assembleDebugAndroidTest` 全部成功。
59 项测试通过（原 47 项 + 11 项结构化仓库测试 + 1 项 v2 迁移测试），没有失败或跳过。
测试覆盖两种动态表结构、字段重排和再次处理、跨任务来源/FK 拒绝、页级事务回滚、来源级联删除、人工数据覆盖保护、文件库重开及 v1→v2→v3 和 v2→v3 迁移。
旧“候选默认需复核”断言按新需求改为“默认未检查”；其余旧行为断言保留。
Lint 0 errors、12 warnings：11 条工具链/依赖版本建议与已有 Uri KTX 提示，没有新增功能缺陷告警。
本机构建使用现有 Android Studio JBR 25（匹配用户的未跟踪 JVM 配置），字节码目标仍为 17。
使用已记录的进程级 Java socket 绕行参数，未修改全局配置。

## 阶段 3：质量检查与复核事务

- 新增独立 ValidationEngine：必填、整数/小数/日期/编号/电话/布尔值、范围、格式、签字存在性人工核对、列模式与长度、全字段及配置组合键重复、页面结构/访问问题。
- 字段格式支持有界、非回溯的正则子集：字面量、字符类、`\d/\w/\s`、数量限定。拒绝分组、回溯引用、环视与分支，输入最多 256 字符，避免用户配置导致灾难性回溯。不是完整 Java Regex 编辑器。
- 规范化仅裁剪两端空白、全角 ASCII 转半角，绝不猜测 O/0 或 S/5。规则只有数据质量含义，不做业务审批。
- 单 Cell 多问题以目标聚合；Issue 保存作用层级、类型、指纹、活动性与状态。重复为行组，原纸问题为来源页。
- Room v4 新增 page_results、validation_issues、review_decisions。确认、修改、保留、无法确认、重复保留/区分/排除在事务内保存并重新计算质量和任务状态。
- 人工动作保留 raw、previous、final 和问题类型；无法确认始终阻止标准导出。排除重复保留数据库证据，不自动删除记录。
- 问题指纹确保规则/数据变化重新打开问题；已消失问题再次出现不沿用旧解决状态。重处理清理该页旧判断；过期 UI 决策可通过 revision/value 拒绝。
- 未处理页面、来源不可用、未解决问题或未确认值均阻止标准导出。导出拒绝时仍提交最新阻塞状态，避免异常回滚将任务留在可导出状态。
- 来源增删/访问检查自然接入已有仓库事务，重新计算质量；处理运行期间禁止变更来源。

### 阶段 3 验证结果

2026-09-09：全量 83 项测试通过，无失败或跳过；Debug、测试 APK、Lint 全部成功。
新增 12 项纯领域规则/聚合测试、11 项质量仓库事务/恢复/门禁测试、1 项冻结 v3 数据迁移测试。
现有 v1/v2 迁移测试继续验证到最新版，v3 迁移保留 raw/normalized/confirmed、原行序与来源区域。
Lint 0 errors、12 warnings，仍为依赖更新和已有 KTX 风格建议。
UI 和真实识别尚未接入这些新服务；后续阶段继续推进，不把本阶段视为 V1 完整交付。

## 阶段 4：真实识别和批处理

### 依赖决策（新增前核对官方资料）

- `com.google.mlkit:text-recognition-chinese:16.0.1`：AndroidX 无中文 OCR 等价实现，选用随包模型而非依赖首次联网下载的版本。官方标注每脚本/ABI 约增加 4 MB；最终 Debug APK 包含多个 ABI，实际包体将在验证中记录。文字框与 Element confidence 均来自真实 API；不是表格模型，也不保证潦草手写准确。
- `androidx.work:work-runtime:2.10.1`：采用官方持久后台工作机制。每任务唯一工作 KEEP，进程重启后可重新执行尚未成功页面。标准 AndroidX 依赖，没有新框架或服务器。
- `androidx.exifinterface:exifinterface:1.4.1`：正确处理原照片旋转/镜像信息，兼容最低 API 26；小型 AndroidX 图像元数据依赖，无模型和网络。
- 所有处理离线执行；Manifest 移除传递依赖可能带入的 INTERNET 权限。WorkManager 自带唤醒、开机恢复等权限，与文件读取权限分开。

官方依据（2026-09-09 读取）：

- https://developers.google.com/ml-kit/vision/text-recognition/v2/android
- https://developers.google.com/android/reference/com/google/mlkit/vision/text/Text.Element#getConfidence()
- https://developer.android.com/jetpack/androidx/releases/work#2.10.1
- https://developer.android.com/jetpack/androidx/releases/exifinterface#1.4.1

### 实际边界

原 URI → 有界 EXIF 正向内存图 → 连续横竖边框检测 → ML Kit 文字框 → 单元格归属 → CandidatePage。
最高解码边长 2200，拒绝超大异常尺寸；一个应用级 Runner 串行执行图片重工作，不复制原图或创建磁盘图像缓存。
当前只支持横平竖直、边框完整、无合并单元格的规整表格。缺边、合并、跨格文字、低可靠表头、无法恢复行列均明确失败。
完全空白的表格槽位不作为记录；部分填写的行保留所有空 Cell。存在墨迹但无法读取时保留空候选并标为难辨，不猜文字。
可靠性由文字最小置信度、区域归属和墨迹检查提供，阈值不是经真实手写数据校准的准确率保证。
SourceRegion 统一对应 EXIF 正向原图，缩放坐标归一化；后续 UI 使用同一加载器显示原纸局部。

页级结果成功即持久化；失败页与正常页分开，结构不同页面不入合并数据。
后续启动默认只处理未成功页，单页重处理有明确入口和人工结果覆盖确认。取消保留已提交页，未完成页记录可重试状态。
Room v5 增加字段配置确认位：识别得到的表头不能自动意味着用户已选择适用的质量规则。
首次识别后用户核对字段设置并保存才可通过配置问题；旧结构化任务迁移后也需核对设置，原值及历史不删除。

### 阶段 4 验证结果

2026-09-09：98 项自动测试全部通过，Debug 和测试 APK 构建成功，Lint 0 errors、15 warnings。
新增 5 项像素网格/文字框结构测试、6 项批处理与中断恢复测试、3 项原生图形解码/EXIF/损坏图片测试、1 项冻结 v4 迁移测试。
原生图形测试发现并修复“只读取尺寸返回 null 被误认为文件打开失败”的真实实现问题；未以默认图形 shadow 的占位图冒充真实解码。
Debug APK 约 55 MiB（含多 ABI、模型与调试依赖），无 INTERNET 权限；WorkManager 的后台恢复权限来自其官方库。
结构测试使用合成像素、批处理测试隔离 ML Kit，不能据此宣称真实拍摄图片或手写识别已经验收。
实际中文识别适配器完成编译和打包；由于仍无设备，真实模型输出与系统后台恢复须在最终真机验收中单独核对。

## 阶段 5：异常作业页面与 XLSX 闭环

首页和详情连接真实记录/待确认统计、后台进度、单页重处理确认、字段设置、异常复核、原纸定位、完整数据搜索编辑和系统文件保存。
复核以目标聚合，支持确认、修改、保持、无法确认、整行/原纸、重复保留/区分/排除；字段配置草稿和选择器回调跨页面重建保留。
编辑后的新值仍受格式/范围/必填规则约束，只有明确确认/保持才接受异常；无法确认持续阻止导出。

XLSX 使用标准库 ZIP/OOXML，无新增表格依赖。动态表头、confirmedValue、跨页顺序和独立复核记录；文本和编号保持字符串，不执行公式样式内容。
导出前刷新来源访问并检查质量，写入后核对快照；写入失败不标记已导出，写入期间数据变化会要求重新导出。取消文件选择不写出，回调在恢复检查期间排队。
WorkManager 使用 Configuration.Provider 官方按需初始化，状态读取异常可重试。

### 阶段 5 验证结果

2026-09-09：110 项自动测试通过，无失败或跳过；Debug、测试 APK 和 Lint 成功（0 errors、18 warnings）。
新增 XLSX XML/ZIP 4 项、导出 Provider/并发数据变化/访问丢失 3 项、工作流状态与回调恢复 3 项、真实 App/Room/导航/文件保存闭环 1 项，以及非法编辑仍阻塞 1 项。
UI 测试使用明确标注的合成候选，未替代实际 ML Kit；测试发现并修复了后台初始化错误、恢复检查期间回调丢失风险与编辑绕过新规则问题。
